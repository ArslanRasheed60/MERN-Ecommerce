{
    "version": 2,
    "builds": [
        {
            "src": "./backend/server.ts",
            "use": "@vercel/ts-node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}